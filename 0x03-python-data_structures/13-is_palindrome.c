#include "lists.h"
/**
 *is_palindrome- checks if a string is palindrome or not
 *@head: pointer that pointes to head pointer
 *Return: 0 if not palindrome, 1 if it is a palindrome
 */

int is_palindrome(listint_t **head)
{
if (*head == NULL)
return (0);

return (1);
}
